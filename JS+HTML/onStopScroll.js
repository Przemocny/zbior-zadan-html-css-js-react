// stwórz skrypt, który działa wg poniższych zasad:
// - dodaje do elementu body klasę "scrolling" jeśli user scrolluje
// - dodaje do body klasę "not-scrolling" jeśli user nie scrollował od przynajmniej 500ms jednocześnie usuwając klasę "scrolling"

// stwórz w elemencie body diva o id "sticky__menu", który działa w oparciu o klasy "scrolling"/"not-scrolling"
// - #sticky__menu ma mieć position:fixed i jest przyklejony do górnej części ekranu
// - #sticky__menu ma mieć width = 100% oraz height = 80px, jeśli występuje w klasie "scrolling"
// - #sticky__menu ma mieć width = 100px oraz height = 50px, jeśli występuje w klasie "not-scrolling"
// - #sticky__menu ma animować zarówno width i height w czasie .3s
